Hello World!

## Firecrawl

This project can use [Firecrawl](https://docs.firecrawl.dev) to scrape and search
web content. Set the `FIRECRAWL_API_KEY` environment variable to authenticate
requests. You can obtain a `FIRECRAWL_API_KEY` at [firecrawl.dev](https://firecrawl.dev).
